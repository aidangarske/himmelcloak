# cargo-deny config. License allow-list plus advisory checks.
[licenses]
allow = [
  "LGPL-3.0-or-later",
  "GPL-3.0-or-later",
  "MIT",
  "Apache-2.0",
  "BSD-3-Clause",
  "ISC",
  "Unicode-3.0",
]

[advisories]
# Use cargo-deny defaults (deny known-vulnerable crates).
